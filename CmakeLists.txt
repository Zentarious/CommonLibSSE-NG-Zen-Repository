cmake_minimum_required(VERSION 3.30.9)

project(CommonLibSSE-NG-Zen-Repository)

# Fetch source from GitLab
vcpkg_from_gitlab(
    OUT_SOURCE_PATH SOURCE_PATH
    REPO https://gitlab.com/colorglass/vcpkg-colorglass.git
    REF "v1.0.0"
    SHA512 0
)

# Configure the project
vcpkg_cmake_configure(
    SOURCE_PATH "${SOURCE_PATH}"
)

# Build and install the project
vcpkg_cmake_install()

# Install the license file (mandatory)
vcpkg_install_copyright(FILE_LIST "${SOURCE_PATH}/LICENSE")

"C:\Coding\vcpkg\scripts\cmake\vcpkg_from_gitlab.cmake"